
machine:
  services:
    - docker
    - docker-compose

test: 
  override:
    - docker --version
    - docker build -t hello-world .
    - docker run -d -p8080:8080 hello-world
    - curl http://localhost:8080

